- OK Create an extract structured data that uses pydentic validators
- OK Add actions to bindings. Each action has its own parameters and can be called by the app. Define the parameters in the yaml
- Images must be used for decision making
